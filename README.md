# partners-deployement-widget
## Deployement Details
1. At the root directory, index.html will display the HTML form seen in the widget
2. Create a custom widget and add the code from widgetCode.html
